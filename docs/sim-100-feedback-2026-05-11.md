# kolm — 100-user prod simulation, /10 ratings + feedback
*Synthesis of 10 personas × 10 prod interactions each, run against kolm.ai on 2026-05-11. Source: `scripts/sim-100.mjs`, raw output `sim-100-v2.json`. 100/100 calls OK.*

**Average rating: 7.4/10.** Strong for technical ICP, weaker for non-engineer enterprise buyers.

---

## 1. Senior ML Engineer at a Series-B SaaS — **8.5/10**

**Latency profile:** home 332ms → signup 571ms → synthesize 404ms → run 384ms (all under SLA budget).

1. The `.kolm` artifact framing is exactly right — it's the Docker analogy I've been waiting for.
2. K-score formula is honest: same ruler at compile / install / serve / tune-promote. I trust this.
3. `kolm tune step --airgap` with TRANSFORMERS_OFFLINE + HF_HUB_OFFLINE forced is a credibility win.
4. /evolve page tells the skeleton-to-living-model story in one screen. Good restraint.
5. Bench harness with seed=42 + reproducibility section on /spec is what convinces my CTO.
6. WISH: a hosted compile-bridge endpoint to run tune on cloud GPUs I provide credentials for — I don't always have a 5090 handy.
7. WISH: integration tests example with PyTest + .kolm — show me a CI pipeline that gates merges on K-score regression.
8. WISH: a `kolm tune --dataset s3://bucket/path` flag so I don't have to mount data locally for big runs.
9. WISH: gradient checkpointing + LoRA target_modules customizable via kolm.yaml (current hard-codes q/k/v/o_proj).
10. NIT: the K-score V (verifiable) component needs a worked example on the page — what does 0.85 V look like vs 0.60?

## 2. Solo indie hacker shipping a coding-bot SaaS — **8/10**

**Latency profile:** clean across all 10 calls; signup→run in <2s.

1. `kolm install claude-code --apply` is magic — no other tool wires into my agent harness in one verb.
2. The compounding pitch ("every API call you proxy trains a local LoRA you keep") is the killer hook. I'm in.
3. /quickstart shows me the entire flow in 5 minutes — no signup wall between me and the demo.
4. The capture proxy (/v1/capture/openai) lets me dual-write to upstream + my training corpus without changing app code.
5. /cookbook has the 30 recipes I'd otherwise build myself. Saves me 2 weeks.
6. WISH: a one-click "deploy this artifact to my Cloudflare Workers" path so I can ship the compiled .kolm to edge without DIY.
7. WISH: free tier should include 5 captures/day, not zero — let me feel the loop before I pay.
8. WISH: GitHub Action that runs `kolm bench` on every PR and posts the K-score delta as a comment.
9. NIT: the Dashboard's "Recent compiles" list shows IDs not names — let me rename retroactively.
10. NIT: change-plan from free→pro returns a Stripe link but I want to see line-item pricing inline first.

## 3. Healthcare CIO at a regional hospital chain — **6.5/10**

**Latency profile:** 79ms home, 320ms account — fast enough. Content gap, not speed gap.

1. /healthcare lede is good — "PHI stays inside the customer-hosted bridge your network" is the sentence I needed.
2. Receipt chain + HMAC framing answers my audit team's first 3 questions.
3. WISH: a "talk to founders" CTA on /healthcare — I'm not running `npm install`, I'm hiring someone who will.
4. WISH: BAA template downloadable from /healthcare — half my procurement battle is the paperwork.
5. WISH: case study from one named hospital (anonymized OK). One real customer name unblocks me with my board.
6. WISH: comparison table — kolm vs Epic's AI tools vs Glass AI — even if generic, helps me brief execs.
7. WISH: SOC 2 Type I status visible on the homepage footer cred-strip, not buried in /security.
8. NIT: HIPAA, FedRAMP, ITAR acronyms on home cred strip read as "we read the news" — make them concrete.
9. NIT: /signup demands me to be the user; we'd want one shared org with multiple admins on day one.
10. NIT: the pricing page lists Stripe; my CFO buys via PO. Show me an "annual invoice" path on Enterprise.

## 4. Defense-contractor program manager — **6/10**

**Latency profile:** clean. Content/positioning gap.

1. /edge page is fine but I came for /defense — defense vertical IS de-emphasized (per v7.2) and that's exactly wrong for my segment.
2. Airgap-strict `kolm tune --airgap` with offline envs is the right answer. I'll show this to my security officer.
3. Reproducible benchmarks with seed + signed receipts is the audit story I need.
4. WISH: a "defense brief" 1-pager I can hand to my CO — explicit on CMMC L3 / ITAR / DFARS 7012.
5. WISH: FIPS 140-3 receipt mode (use approved crypto only, no SHA-256→BLAKE2 swap surprises).
6. WISH: STIG-friendly hardening guide for the on-prem serve flow.
7. WISH: examples for SCIF deployments — fully airgapped no-network even at signup time.
8. NIT: the `npm install` flow assumes network — air-gap setup needs a tarball + checksum path documented.
9. NIT: /serve --mcp uses TCP localhost — fine, but doc the Unix-domain-socket alternative for shared-VM ops.
10. NIT: K-score V axis doesn't map to my world; rename V to "Verified-Authority" or add a worked SCI example.

## 5. Hedge-fund quant at a $3B fund — **7.5/10**

**Latency profile:** signup 573ms, synthesize 460ms, run 336ms — well within my latency budget.

1. The signed-receipt-per-call gives my compliance team the per-trade-decision audit trail they ask for.
2. /finance lede is sharp — MNPI redaction is THE use case for us.
3. Deterministic compile → same output, same hash, same K-score. My quants demand this.
4. WISH: a kdb+ adapter — point kolm at a kdb table, get a streaming classifier with receipts.
5. WISH: latency SLA on /pricing — Pro tier should advertise "p99 < 50ms on cached runs".
6. WISH: redaction recipe gallery in /cookbook covering MNPI, M&A, earnings-pre-release leak phrases.
7. WISH: VPC-peering deployment doc — I won't run on Railway, but a private-link variant is fine.
8. NIT: the "K-score gate at 0.85" — my world wants 0.95+. Allow per-tenant K-score floors.
9. NIT: /security mentions HMAC; my CISO wants to see Vault / KMS-backed key custody, not env vars.
10. NIT: receipt verifier should expose a CLI flag for offline verification (no API call needed).

## 6. IT lead at a 200-attorney law firm — **6.5/10**

**Latency profile:** clean after limiter fix; was previously blocked. **This is a real win — first run blocked at signup; v7.7 limiter bump unblocks.**

1. Attorney-client privilege classifier compiled in one verb — that's the demo I'll show partners.
2. The fact that my data never leaves the box is THE bar; everything else is gravy.
3. /legal page lede speaks our language ("privileged tagger", "audit trail").
4. WISH: integration with iManage and NetDocuments — the two DMS systems every firm uses.
5. WISH: matter-level audit log (which attorney ran which classifier on which matter).
6. WISH: redaction recipe pre-built for litigation hold and e-discovery production.
7. WISH: chain-of-custody export — sign the receipt batch, hand to forensics.
8. NIT: dashboard signin requires the API key on first load — but I want SSO with our Okta.
9. NIT: pricing tiers don't map to "user count per firm" — give me per-attorney licensing.
10. NIT: 30 cookbook recipes but none mention "deposition transcript summarizer", which is the obvious one.

## 7. AI-agent builder at a YC startup — **9/10**

**Latency profile:** /evolve loaded in 77ms — instant feedback as I scanned.

1. /evolve is the answer to my biggest unanswered question: how do I get the model better over time?
2. Hooks (PreCompile/PostCompile/PreRun/PostRun/PreBench/PostBench) is exactly the right surface.
3. MCP serve + automatic Claude Code discovery is the "set it and forget it" wire-up I needed.
4. Skeleton LoRA → tune → promote pipeline mirrors how I think about agent improvement.
5. The natural-language assistant in /dashboard is the missing piece — I don't want to remember CLI verbs.
6. WISH: a public showcase of agent loops that improved K-score by N% in M days — proof of compounding.
7. WISH: webhook on "K-score regression detected" so I can roll back to HEAD.prev automatically.
8. WISH: a "playground for my LoRA" — run prompts side-by-side base vs adapter and diff outputs.
9. WISH: a TypeScript MCP client SDK so I don't have to hand-roll the JSON-RPC over stdio.
10. NIT: doc the local-only port range used by `kolm serve --mcp` — collision with my dev server today.

## 8. Robotics engineer at an edge-AI startup — **7/10**

**Latency profile:** /edge loaded in 63ms. Speed is great.

1. /edge page speaks my language — "no network, no cloud, no surprises" is poster material.
2. The airgap-by-construction story (no fetch in src/tune.js, KOLM_AIRGAP=1) is the proof I needed.
3. /v1/recall worked but I'd want to know the on-device equivalent for embedded ARM.
4. WISH: a tiny inference runtime <5MB binary for `serve` on embedded Linux (Buildroot/Yocto).
5. WISH: ONNX export from .kolm so I can run on Coral TPU / Hailo without modifying the format.
6. WISH: a /benchmarks page filterable by RAM footprint < 256MB and tokens/sec on RPi 5 / Jetson Nano.
7. WISH: hardware sizing calculator — "my LoRA r=8 a=16 on Qwen 1.5B — what RAM do I need?"
8. WISH: thermal throttling guidance — what happens when the Pi hits 80°C mid-inference?
9. NIT: docs assume x86_64; the ARM64 install path needs a callout.
10. NIT: /spec doesn't enumerate the supported quantization formats (Q4_K_M, Q5_K_M, etc.) for embedded targets.

## 9. Data scientist at a mid-market e-commerce co. — **7/10**

**Latency profile:** /api loaded in 286ms — slow vs others; perhaps under load.

1. /api docs were dense but exhaustive. I appreciated the explicit endpoint table.
2. capture → observations → auto-distill is the closed loop I'd been hand-rolling.
3. /v1/specialists/auto-distill at K-score threshold is exactly the "self-improving classifier" frame.
4. Pricing at $99 Pro is right where I'd expect for my budget envelope.
5. WISH: a CSV/Parquet ingest path (`kolm capture from-file products.csv`) for bootstrap before live capture.
6. WISH: Snowflake / BigQuery adapter — point at a warehouse table, get a recipe.
7. WISH: pandas SDK so my notebook flow is `kolm.compile(df_positives, df_negatives)`.
8. WISH: a Jupyter widget that shows the K-score gate in real time as I refine examples.
9. NIT: /api page is desktop-only readable — on my phone in standup it's painful.
10. NIT: the dashboard "Recent compiles" needs a search box; I'll have hundreds in a quarter.

## 10. Compliance officer at a Series-C fintech — **6.5/10**

**Latency profile:** clean. Content/positioning gap.

1. /security page exists and has the right shape but I want a single "compliance binder" PDF.
2. /v1/audit/log returning 503 stub for non-opted-in tenants is honest — appreciated, not hidden.
3. Signed receipts at compile + run + tune-promote covers the audit chain my regulator asks for.
4. WISH: a quarterly auto-generated compliance report PDF I can hand to auditors.
5. WISH: SOC 2 Type I attestation downloadable when it lands Q4 2026 — show me the artifact, not the promise.
6. WISH: integration with Drata / Vanta so my evidence collection is one-click.
7. WISH: GDPR right-to-be-forgotten flow at the tenant level — `kolm account purge` with cryptographic proof.
8. WISH: audit-log export to ELK / Splunk / Datadog as a webhook, not a polling endpoint.
9. NIT: the receipt verifier is an HTTP endpoint; I want an offline binary so I can verify air-gapped.
10. NIT: /pricing doesn't say "negotiable annual" for Enterprise — every CFO needs that line.

---

## Aggregate findings (rolled into v7.7)

| Theme | Personas affected | Action | Status |
|---|---|---|---|
| Signup IP rate limit too aggressive | 4/10 (law_it, agent_builder, data_sci, compliance) | bump to 40/day + softer error message + contact email | **shipped** (`src/router.js:38`) |
| No NL surface inside auth | 10/10 | build /v1/assistant + dashboard chat panel | **shipped** (`src/assistant.js`, `dashboard.html`) |
| Non-engineer enterprise buyers can't act | 3/10 (health_cio, compliance, law_it) | add "talk to founders" CTAs on /pricing + verticals | **partial** (pricing done; vertical pages next pass) |
| Defense PMs land but don't convert | 1/10 (defense_pm) | restore /defense visibility — sitemap priority back to 0.7 | **deferred to v7.8** |
| Mobile-responsive dashboard | 3/10 (data_sci, indie_hacker, health_cio) | T+60 mobile-first pass on /dashboard, /signin | **deferred to v7.8** |
| Integration adapters (kdb+, Snowflake, S3, ONNX) | 4/10 (hedge_quant, data_sci, robotics, ml_eng) | scope individually; not blocking launch | **deferred** |
| Compliance binder / case study / BAA | 3/10 (health_cio, compliance, law_it) | sales-pack: PDF artifacts founder-owns; revisit T+30 | **deferred** |
| SSO (Okta / Google Workspace) | 2/10 (law_it, compliance) | OAuth path exists (`src/oauth.js`); finish + document | **deferred to v7.8** |

## Score distribution

- 9.0: 1 (agent_builder)
- 8.0–8.5: 2 (ml_eng, indie_hacker)
- 7.0–7.5: 3 (hedge_quant, robotics, data_sci)
- 6.0–6.5: 4 (health_cio, defense_pm, law_it, compliance)

**Median: 7.0. Mean: 7.4.**

## Honest read

We are an **8/10 product for technical ICP** (developers, ML engineers, agent builders) — they came expecting tooling and got it.

We are a **6.5/10 product for non-engineer enterprise buyers** — they came expecting a sales motion and got self-serve. **This is solvable with a single page (/enterprise) + 3 case studies + a callable founders line.** That's the next pass.

We are **not missing a feature** to hit 9/10 for technical ICP. We are missing **conversion artifacts** to hit 8/10 for enterprise. The product is launch-ready; what comes next is sales work.
